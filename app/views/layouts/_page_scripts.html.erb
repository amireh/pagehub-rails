<script>
  if (PHLegacy.Application) {
    runPageScripts(PHLegacy.Application);
  }
  else {
    console.log('queuing');
    PHLegacy.Application.once('ready', runPageScripts);
  }

  function runPageScripts(application) {
    console.log('running page scripts')

    <%= (yield :deferred_js).gsub(/\<\/?script\>/, '').strip.html_safe %>
  }
</script>